<Page
    x:Class="AppExtensionHost.Views.ExtensionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="ExtensionDetailTemplate">
            <Grid Width="400" Height="300" Margin="12" Background="LightCyan">
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid
        x:Name="ContentArea"
        Margin="{StaticResource MediumLeftRightMargin}">
        <ItemsControl
            ItemTemplate="{StaticResource ExtensionDetailTemplate}"
            ItemsSource="{x:Bind ViewModel.Extensions}" />
    </Grid>
</Page>
